import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const FeaturedRow = ({ tile, desc }) => {
  return (
    <View>
      <Text>FeaturedRow</Text>
    </View>
  );
};

export default FeaturedRow;

const styles = StyleSheet.create({});
